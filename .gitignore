# Build artifacts and binaries
certs/
builddir/
*.tar.gz
!edr/uptycs-complete*.tar.gz
*.tar
*.qcow2

# Secrets and credentials
*.pem
*.key
*.crt
*secret*
.env

# Uptycs specific - exclude binaries and configs with potential secrets
scripts/coco/podvm/luks-config.tar.gz
# NOTE: Temporarily allowing uptycs tarballs for container builds - see TODO.md

# Build output and logs
build/*.log
*.log

# IBM Cloud temporary files
.ibm_cloud_image_id

# Upstream reference repositories (cloned for reference only)
upstream-repos/
